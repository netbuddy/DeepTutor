[project]
name = "deeptutor-ext"
version = "0.1.0"
description = "DeepTutor 本地扩展：课程导入与 notebook 执行"
requires-python = ">=3.11"
dependencies = []

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[tool.setuptools]
packages = ["deeptutor_ext", "deeptutor_ext.tools"]
